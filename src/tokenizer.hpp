#ifndef TOKENIZER_HPP
#endif TOKENIZER_HPP

#include <string>
/*
enum TokenType{
		eFunctionName = 0,
		eFunctionParametersListBegin,
		eVariableType,
		eObjectName,
		eAssignment,
		eNumber,
		eString,
		eFunctionParametersListEnd,
		eScopeBegin,
		eFunctionCall,
		eAddition,
		eSubtraction,
		eMultiplication,
		eDivision,
		eIncrement,
		eDecrement,
		eSemicolon
	}
	struct Token{
		TokenType type,
		std::string content;
	}

class Tokenizer{
private:
	static inline std::string knownKeywords[] = {};
public:
	static void stringToTokens(std::string& source, std::vector<Token>& pOutput){
		std::string wordBuffer;
		for(size_t i = 0; i<source.length(); i++){
			while(source[i]!=' ' || sourcce[i]!=';' || source[i]!='{' || source[i]!='}'){
				wordBuffer+=source[i];
				i++;
			}
			
		}
	}
private:
	void wordToToken(std::string& word, Token& pOutput){
		pOutput = {};
		
	}
};
*/
#endif
